# Jetson Voice Assistant
# Tested on: Jetson Orin Nano 8GB, JetPack 6, Python 3.10

# Core
pyyaml>=6.0
rich>=13.0
typer>=0.9.0
psutil>=5.9.0

# Audio I/O
sounddevice>=0.4.6

# VAD — Silero neural voice activity detection (much better than energy-only)
silero-vad>=5.1

# LLM client (OpenAI-compatible API)
httpx>=0.24.0

# STT — faster-whisper (requires CTranslate2 with CUDA, see README)
faster-whisper>=1.0.0

# TTS — Kokoro (GPU via ONNX Runtime, runs in subprocess for GPL isolation)
kokoro-onnx>=0.4.0

# Testing
pytest>=7.0

# Jetson-specific — ONNX Runtime GPU (replaces the PyPI onnxruntime wheel):
#   pip uninstall -y onnxruntime onnxruntime-gpu
#   pip install --force-reinstall numpy==1.26.4 onnxruntime-gpu==1.23.0 \
#     --extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
# Both packages installed simultaneously causes the CPU-only wheel to win.
